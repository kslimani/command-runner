#!/usr/bin/env node
var AsyncRunner = require('../lib/AsyncRunner')

// var file = "test.json"
var cfg = {}
var runner = new AsyncRunner(cfg)
// runner.debug(true)
// runner.run()
console.log('WIP...')
